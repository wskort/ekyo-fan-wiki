---
aliases:
  - VM (Virtual machine)
  - Virtual machine
tags: Techniek
---
# Virtual machine (VM)
On [[My laptop]], I run the [[Local deployment]] with the [[Hypervisor]] VMware (15). 

`ALT-CTRL` to 'release' VM (escape with besturing muis/keyboard)

VMware installs tools on the VM; kan met top balk van VMware zelf (ipv de VM) bestuurd worden.