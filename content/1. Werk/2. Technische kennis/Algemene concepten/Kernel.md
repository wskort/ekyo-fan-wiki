---
aliases:
  - Monolithic Kernel
  - Microkernel
tags: Techniek
---
# Kernel
A mandatory part of each [[1. Werk/2. Technische kennis/Algemene concepten/OS|OS (Operating system)]], its core, is **the kernel**. Usually, it's one of the first programs that loads when you turn on your computer. It provides all the necessary means to run the programs you want.

Typically, when you start your OS, you see **the [[GUI|GUI (Graphical User Interface)]].** It is the type of interface that allows users to interact with the device using graphical icons and audio indicators. Another way to interact with the OS is to use commands in a text-based terminal known as a **[[CLI|CLI (Command Line Interface)]].**

There are two types of kernels, known as monolithic and microkernels. A **monolithic kernel** is a large program that performs most of the OS functions. At the same time, a **microkernel** performs only a small subset of the operating system functions, but we can extend it with additional modules known as **drivers**.

A kernel is the main part of any [[1. Werk/2. Technische kennis/Algemene concepten/OS|OS (Operating system)]] providing programs with coordinated access to computer resources.